# Copy this file to .env and fill local values.

# Path can be absolute, or relative to project root / backend directory.
GOOGLE_SHEETS_CREDENTIALS_PATH=backend/google-service-account.json

# Do not commit the real sheet id in public repositories.
GOOGLE_SHEETS_SPREADSHEET_ID=your_google_sheet_id_here

# Sheet tab used by the Situations page.
GOOGLE_SHEETS_SITUATIONS_SHEET=Situations

# Portal user resolution for local development and future portal mount.
PNL_PORTAL_DB_PATH=../SoftPortal/backend/data/portal.db
PNL_DEV_USERNAME=
# Optional hard override. Leave empty if username lookup should be used.
PNL_DEV_USER_ID=

# One-time owner for old local rows created before portal users existed.
# Set this explicitly on the machine that owns the legacy local database.
PNL_LEGACY_OWNER_USERNAME=
PNL_LEGACY_OWNER_USER_ID=
